import { Routes } from '@angular/router';
import { AdminReservationsPage } from './admin-reservations-page';

export const ADMIN_RESERVATIONS_ROUTES: Routes = [
  { path: '', component: AdminReservationsPage, title: 'Reservas | Admin' }
];
